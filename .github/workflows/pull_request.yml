name: pull_request

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - .devcontainer/**
      - .vscode/**
      - docs/**
      - README.md
      - LICENSE
      - .giitignore
      - .github/workflows/**

jobs:
  
  pull_request:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: '.python-version'

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          # Optional: pin for reproducibility, e.g. "0.5.24"
          version: "latest"
          enable-cache: true

      - name: Install dependencies
        run: |
          uv sync --group dev --frozen

      - name: Run tests
        run: |
          make test
